import 'package:flutter/material.dart';

class SheduleFinance extends StatelessWidget {
  const SheduleFinance({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
